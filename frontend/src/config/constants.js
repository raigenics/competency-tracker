/**
 * Application-wide constants
 * 
 * Single source of truth for configuration values used across the UI.
 */

/**
 * Super Admin email for skill requests.
 * Users seeing a skill not in the system should contact this email.
 */
export const SUPER_ADMIN_EMAIL = 'competency-admin@siemens.com';

/**
 * Default page size for paginated lists
 */
export const DEFAULT_PAGE_SIZE = 10;

/**
 * Maximum number of skill suggestions to show in autocomplete
 */
export const MAX_SKILL_SUGGESTIONS = 10;
